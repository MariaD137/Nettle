node_modules/
dist/
*.log
nettle-report.json

# Runtime Tier 2 SQLite file (created wherever NETTLE_DB_PATH points, default
# ./nettle.db) — NOT the bundled OSV vulnerability database, which is real
# shipped data and must stay tracked. Keep this pattern specific.
/nettle.db
/nettle.db-journal
/nettle.db-wal
/nettle.db-shm
